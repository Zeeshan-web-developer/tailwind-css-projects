var User = require('../lib/users.js').User;
var crypto = require('crypto');


User.find({user: "julien"}, function(user) {
    var password = "inc0nueE";
    console.log(user.auth(password));
    process.exit(0); 
});